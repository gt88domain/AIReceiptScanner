# Account App profile

Use this profile for accounts, private content, saved items, and uploads. It keeps Jobs infrastructure for background work but does not enable Billing, Credits, or mobile payments. It needs D1, R2, a Queue with DLQ, and a Cron trigger.

The examples are intentionally non-deployable. Review and copy them manually; this profile never edits a project configuration or creates Cloudflare resources.
