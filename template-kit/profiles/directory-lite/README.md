# Directory Lite

`directory-lite` is the supported profile for an Admin-backed directory or content product with no asynchronous Jobs. It requires only D1: do not create or bind a Queue, DLQ, Cron trigger, R2 bucket, or payment provider.

Copy the examples deliberately into an adoption branch and replace every `replace-` value. They are non-deployable placeholders and do not create cloud resources.
