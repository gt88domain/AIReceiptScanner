# Packages

Shared workspace packages.

- `api-client`: typed API client helpers.
- `app-config`: non-secret shared business config.
- `i18n`: shared messages and locale helpers.
- `shared`: runtime-neutral utilities.

Do not add a shared package just because code might be reused. Promote only
after repeated use or a stable cross-app contract.
