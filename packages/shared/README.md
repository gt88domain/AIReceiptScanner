# shared

Runtime-neutral utilities and types shared by apps and packages.

Use this package for small pure helpers that are genuinely shared.

Do not put server-only code, browser-only code, D1 access, provider clients, or
secrets here.
