# Worker runtime secrets for the rotation/write path only (not daily deploys).
# Usage:
# 1) cp .env.production.example .env.production
# 2) Fill runtime secrets in .env.production (empty optional keys are skipped)
# 3) pnpm run secrets:push:production  (validates plaintext, then filtered bulk)
# Public production URLs and OAuth client IDs belong in apps/server/wrangler.jsonc vars.

# This must remain production. Wrangler is the resource identity source.
ENVIRONMENT=production

# Provider docs: https://better-auth.com/docs/installation
BETTER_AUTH_SECRET=

# Administrator access - comma-separated emails, deployed only as a Worker secret.
# Do not add this to wrangler.jsonc vars or any VITE_ variable.
ADMIN_EMAILS=

# Github OAuth
# Provider console: https://github.com/settings/developers
GITHUB_CLIENT_SECRET=

# Google OAuth
# Provider console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_SECRET=

# Apple OAuth / Sign in with Apple (native idToken flow)
# Docs:
# - https://docs.expo.dev/versions/latest/sdk/apple-authentication/
# Native iOS bundle id (example: com.example.app), should match app.json ios.bundleIdentifier
APPLE_APP_BUNDLE_IDENTIFIER=

# Resend Email
# Provider console: https://resend.com/api-keys
RESEND_API_KEY=
# Required for production when Resend is enabled. Example: noreply@your-domain.com
EMAIL_FROM=<verified-sender@production-domain>
# Required only when Contact Form is enabled; this value is never public runtime config.
CONTACT_RECIPIENT=

# Optional Cloudflare Turnstile secret for Contact and Newsletter forms.
# Configure only together with apps/web VITE_TURNSTILE_SITE_KEY.
TURNSTILE_SECRET_KEY=

# R2 Storage
# Public URL for accessing R2 stored files (e.g., https://pub-xxx.r2.dev or your custom domain)
R2_PUBLIC_URL=

# Stripe
# Provider docs: https://stripe.com/docs/keys
# Prefer a least-privilege rk_live_ key; sk_live_ remains supported.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# RevenueCat
REVENUECAT_WEBHOOK_SECRET=
