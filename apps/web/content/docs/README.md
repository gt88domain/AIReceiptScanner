# Product documentation collection

This collection is owned by the adopting product. Add only product-owned MDX
documents after enabling the literal `docs` flag in
`packages/app-config/src/content-flags.ts`. Each enabled locale needs its own
`index.mdx` landing document and matching published slugs; a missing locale
intentionally returns 404. Do not add template examples or copied marketing
content here.
