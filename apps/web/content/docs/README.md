# Product documentation collection

This collection is owned by the adopting product. Add only product-owned MDX
documents after enabling `features.docs` in `packages/app-config/src/product-config.ts`.
Do not add template examples or copied marketing content here.
