# Product blog collection

This collection is owned by the adopting product. Add only product-owned MDX
articles after enabling the literal `blog` flag in
`packages/app-config/src/content-flags.ts`. Publish the same article slug and
its author/category records in every supported locale directory before setting
it as published; a missing locale intentionally returns 404. Do not add
template examples or copied marketing content here.
