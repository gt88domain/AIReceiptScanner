## 1. Implementation
- [x] 1.1 Add `payments` and `storage` modules under `packages/app-config/src`.
- [x] 1.2 Add `./payments` and `./storage` subpath exports in `packages/app-config/package.json`.
- [x] 1.3 Migrate all server and web imports to `@repo/app-config/payments` and `@repo/app-config/storage`.
- [x] 1.4 Remove `packages/payments-config` and `packages/storage-config`.
- [x] 1.5 Update config architecture documentation.

## 2. Validation
- [x] 2.1 Run repository-wide TypeScript check and resolve all type errors.
