# v1.2.0 收官 Prompt：提交、合并、发布

> 前置：`codex/apps-modules-v1.2` 上的全部改动未提交，已通过所有者人工验收（零模块时导航零回归）与审查。
> 本任务只做 Git 收口，不改任何代码。任何测试失败都停下来报告。

## 步骤

1. 在 `codex/apps-modules-v1.2` 上整理 Conventional Commits，建议拆分：
   - `feat(backoffice): add build-time module registry` — backoffice.ts / backoffice-registry.ts / 侧边栏合入
   - `feat(admin): add guarded module route helper` — createAdminModuleRoute + (modules) 目录 + 边界规则
   - `test(backoffice): add module contract tests` — backoffice.test.ts + web vitest 配置
   - `docs(backoffice): document module adoption` — backoffice-modules.md + prompt 修正 + ia 矩阵更新
2. rebase 到最新 origin/main 后 fast-forward 或单次 merge 合入 main；合并后运行
   `pnpm test && pnpm check-types && pnpm lint && pnpm fmt:check && pnpm check:boundaries && pnpm perf:budget`，全绿才继续。
3. 走 release-please 既有流程，接受自然版本号（预计 v0.11.0），不追数字。
4. push origin main。这是唯一允许的远端写操作。
5. 输出报告：commit 列表、门禁输出摘要、release PR 链接。

## 明确不做

- 不改代码、不删除源分支、不动任何下游仓库。
- 不把任何真实项目模块带进上游。
