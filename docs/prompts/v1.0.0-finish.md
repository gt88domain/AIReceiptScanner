# v1.0.0 收官 Prompt：提交、合并、发布

> 前置：v0.9 改动在 `codex/backoffice-preview`（checkpoint `c569cd1`）；
> v1 改动在 `codex/backoffice-ia-v1`，全部未提交，且已通过所有者人工验收与审查。
> **注意：`codex/backoffice-ia-v1` 基于 `c569cd1`，v0.9 已包含在它的历史中。**
> 本任务只做 Git 收口，不改任何代码。任何测试失败都停下来报告，不要「顺手修」。

## 版本号策略（已签字）

- 文档中的「v0.9 / v1.0」是**里程碑代号**，不是 git tag。
- 实际 semver tag 由 release-please 按 Conventional Commits 自然产生（预计 0.9.0），
  **不强行制造 1.0.0**，不写伪造的 BREAKING CHANGE，不改 release-please 配置。

## 步骤

1. **整理 v1 提交**（在 `codex/backoffice-ia-v1` 上），按 Conventional Commits 拆分：
   - `feat(backoffice): finalize user and admin navigation` — Account 分组、Purchases、Help、显隐派生
   - `feat(admin): read-only payments page and action-queue overview` — Payments 页、Overview 队列、migration 状态
   - `refactor(admin): fold integrations into system providers` — Integrations 并入 System + 重定向
   - `fix(help): disable contact form when delivery is unavailable` — Help 降级
   - `test(integration): align avatar expectation with https-only policy` — 头像测试期望修正（独立 commit）
   - `docs(backoffice): document ia decisions and visibility matrix` — backoffice-ia.md 等文档
   - 允许按实际文件归属微调拆分，但测试修正必须独立 commit。
2. **单次合并**：把 `codex/backoffice-ia-v1` 合入 main（v0.9 随其历史一并进入）。
   - 合并前确认 `c569cd1` 在该分支历史中（`git merge-base --is-ancestor c569cd1 HEAD`）。
   - 合并后在 main 上运行 `pnpm test && pnpm check-types && pnpm lint && pnpm fmt:check`，全绿才进行下一步。
   - 不需要单独合并 `codex/backoffice-preview`；该分支保留作回溯，不删除。
3. **发布**：走仓库既有 release-please 流程，接受它产生的版本号（预计 0.9.0），不手工改版本号文件。
4. **push 到 origin/main**。这是本任务唯一允许的远端写操作。
5. 输出报告：commit 列表、合并方式与 `merge-base` 验证结果、测试输出摘要、release PR 链接。

## 明确不做

- 不改代码、不 rebase 改写已推送历史、不动其他分支、不动下游任何仓库。
- 不删除 `codex/backoffice-preview` 与 `codex/backoffice-ia-v1` 分支（留作回溯）。
- 不追求特定版本号数字。
